package jvmusin.customaggregations

/**
 * A single header from [Contents].
 *
 * @property level Indentation level of `this` header.
 * @property title Title of `this` header.
 */
data class Header(val level: Int, val title: String)
